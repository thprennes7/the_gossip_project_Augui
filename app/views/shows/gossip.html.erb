<h1>Potin n° <%= @current_gossip.id %> de <%= link_to "#{@current_gossip.user.first_name}", profile_path(@current_gossip.user.id) %></h1>
<h2><%= @current_gossip.title %></h2>
<p><%= @current_gossip.content %></p>
<p><em>Date de création : <%= @current_gossip.created_at.asctime %></em></p>
